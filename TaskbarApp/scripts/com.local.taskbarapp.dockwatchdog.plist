<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>Label</key>
    <string>com.local.taskbarapp.dockwatchdog</string>

    <key>ProgramArguments</key>
    <array>
        <string>__SCRIPT_PATH__</string>
    </array>

    <!-- Запуск сразу при загрузке LaunchAgent (т.е. при логине пользователя) -->
    <key>RunAtLoad</key>
    <true/>

    <!-- Плюс каждые 15 секунд, пока пользователь залогинен -->
    <key>StartInterval</key>
    <integer>15</integer>

    <key>StandardOutPath</key>
    <string>/tmp/taskbarapp-dockwatchdog.log</string>
    <key>StandardErrorPath</key>
    <string>/tmp/taskbarapp-dockwatchdog.log</string>
</dict>
</plist>
